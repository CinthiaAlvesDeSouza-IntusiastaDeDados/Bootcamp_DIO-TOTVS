saldo = 1000
limite = 1000

print(saldo is limite) # Verifica se as variáveis saldo e limite referenciam o mesmo objeto na memória
print(saldo is not limite) # Verifica se as variáveis saldo e limite não referenciam o mesmo objeto na memória
